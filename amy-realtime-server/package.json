{
  "name": "amy-realtime-server",
  "version": "1.0.0",
  "private": true,
  "description": "Dashboard-only realtime proxy for Ask Amy (browser <-> Deepgram Voice Agent). Separate from voice-server / the phone pipeline.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ws": "^8.18.0"
  }
}
